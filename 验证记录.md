# 第二版验证记录

验证日期：2026-09-15。没有部署到外部服务器。

## 本次修改的检查

- 顶部微调：进度条改为实际宽度动画，避免横向缩放压扁圆头；能量闪电缩为原尺寸的65%。在390×844视口检查黄色短进度条，确认完整圆头和图标比例，脚本与素材检查通过。

- 14项自动测试通过，新增整道配对只增加一次连击、每题递减进度、错题重练补足进度，以及9道题无重复内容的检查。
- 浏览器实测配对前三组完成时连击仍为1，第四组完成后为2；另一道配对制造错误后归零，完成剩余配对仍为0。
- 答对后读取到连击提示和过渡中的进度值，动画结束后提示文字清空、透明度为0；下一题保留同一个顶部区域，过渡不中断。
- 390×844与320×740视口检查翻译长句：8个词块能够全部选中，词块底部与所属行横线位置一致；已选词块没有整排下跳动画。检查了移回词库及连续选词。
- 修复缺录音提示条拦截小屏下排词块点击的问题，提示条现在不拦截操作。
- 320×740视口的长句词库、检查按钮可完整显示，页面宽高均为320×740；390×844长句也无溢出。
- 熊二和拽姐跟读只保留气泡内一个播放入口；工作人员提示保留，角色无跟读动画。
- 浏览器完成9道不同题目，制造一次配对错误及一次翻译首答错误；翻译重练再次错误后直接结算为7/9、78%，进度为100%。
- 最终版本另完成一轮全部正确，结算9/9、100%，最后连击为9。
- 页面无console error/warn。界面检查使用Chromium手机视口模拟，尚未在实体手机或微信内核验收。

## 第一版已验证、本次保留的功能

- 10项自动测试覆盖固定9题、计分与连击、配对错误恢复、翻译最多重练一次、重练再错也结束、跟读一次继续、后台时间排除、广告真实播放时长、禁止以快进/后台等待满足广告条件、媒体计时抖动。
- 浏览器中完成全部9题，核对9/9、100%的结算。
- 浏览器中制造翻译首答错误，最后重练再次答错，核对直接结算为8/9、89%，没有再次重练。
- 浏览器中制造配对错误，确认红色反馈后可继续，已成功配对保留。
- 熊二、拽姐两道跟读均有工作人员提示，点击一次继续即可进入下一题；角色动画状态为none。
- 390×844及320×740测试视口：无横向溢出，跟读工作人员提示与继续按钮同时可见。
- 长句翻译在390×844视口中词库与底部按钮可完整显示。
- 广告占位完整倒计时后自动进入二维码占位页。
- 临时WAV测试文件能触发真实播放；缺失录音会提示，不会调用合成语音。
- 临时15秒H.264/AAC MP4真实播放，原生拖动控件关闭，完整播放后自动进入图片页。
- 最终二维码图片加载分支使用临时参考图片验证；这不是二维码识别测试。
- 浏览器未发现页面console error/warn。
- 可选WebMCP读状态/提交翻译工具通过真实浏览器调用验证；错误词块输入拒绝且没有更改状态。

临时音视频与图片引用均已移除，最终媒体清单恢复为待提供状态。

## 本次验证的范围

上述手机尺寸检查使用内嵌Chromium浏览器的视口模拟，不等于已在实体iPhone、安卓或微信内核中验收。正式社员音频、15秒广告和可识别的二维码尚未提供，需要接入后在活动实际设备上再试听和扫码确认。

角色动作是截图形象配合CSS位移/姿态切换，尚无逐帧重绘或原版骨骼动画。第二版已将复用题目改为不重复的测试语料，材料说明中的录音编号同步更新。
