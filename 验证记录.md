# 验证记录

验证日期：2026-09-15。应用已部署到 https://conscient.hk.cn/yue/。

## 自动检查

- 14项测试覆盖固定9题、整题计分和连击、配对错误恢复、翻译最多重练一次、跟读一次继续、后台时间排除、递减进度及题目无重复内容。
- 广告测试覆盖实际播放时长、结束条件，以及快进、后台等待、加速播放与正常媒体计时抖动。
- 项目检查覆盖题库结构、脚本语法、页面入口、角色图片和已接入素材引用。

## 页面验证

- 完成全部9题并核对9/9、100%，最后连击为9。
- 制造一次配对错误与一次翻译首答错误，翻译重练再次错误后直接结算为7/9、78%，最终进度100%。
- 配对前三组成功不增加整题连击，第四组完成后才增加一次；中途配错后连击归零，该题完成也不补加。
- 连击短暂显示后清空；切题保留顶部区域，使动画可以自然结束。
- 进度条使用实际宽度动画，末端保持圆头；在390×844视口检查过黄色短进度条及能量闪电比例。
- 390×844和320×740视口中的翻译长句可选中全部8个词块，词块底部贴齐横线，词库和按钮完整显示。检查了词块移回及连续点击，缺录音提示不拦截点击。
- 熊二和拽姐跟读各有一个气泡内播放入口，保留工作人员提示和一次继续操作，角色没有跟读动画。
- 广告占位完整倒计时后自动进入二维码占位页。
- 使用临时WAV文件验证实际音频播放；使用临时15秒H.264/AAC视频验证完整播放及图片页跳转。正式媒体清单已恢复为待提供状态。
- 二维码图片加载分支使用临时参考图片验证过，不属于二维码识别测试。
- 可选WebMCP状态读取和翻译提交工具验证通过；非法词块输入被拒绝且不改变状态。
- 浏览器未发现页面console error/warn。

## 服务器部署与更新

- SSH连接、sudo部署权限及Nginx配置检查通过，博客配置已经备份。
- 新增 `/yue/` 路径后等待新工作进程就绪，再校验网页；博客原有反代保留。
- 从本机公网访问，12个线上文件与本地SHA-256完全一致，JavaScript/CSS资源类型正确，响应包含 `Cache-Control: no-cache`。
- `/yue` 正确跳转到 `/yue/`，博客首页仍返回200。
- 线上390×844视口中首题、角色图片和选词交互正常，无横向溢出或页面console错误。
- 实际运行一次完整OTA流程，上传、校验、切换、更新后的HTTPS校验和备份均通过。
- 3项独立临时目录测试验证正常发布与回退、文件校验失败及HTTP检查失败时保留原网页、拒绝越界归档路径；测试不访问真实网页目录。

## 验证范围

手机尺寸检查使用Chromium视口模拟，尚未在实体iPhone、安卓及微信内核中验收。正式社员录音、广告和二维码需要接入后在活动实际设备上试听、看完广告和扫码确认。

角色为截图裁切配合CSS动画，没有原版骨骼动画。题库为9道不重复测试题，正式粤语写法、粤拼及词块拆分可由社员校订。
