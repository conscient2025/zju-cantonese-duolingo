# 部署与手动 OTA

在线地址：**https://conscient.hk.cn/yue/**。

应用已部署到 `admin@118.178.91.193`（Ubuntu、直接安装Nginx）。博客继续由 `http://127.0.0.1:3000` 提供内容；粤社应用由Nginx直接读取静态文件。

这里的“手动OTA”指通过SSH上传本机最新网页，然后切换线上内容。无需重新安装手机应用，用户刷新网页即可获取更新。GitHub只保存项目代码，提交或推送不会自动部署。

## 日常更新：一条命令

在这台电脑的 PowerShell 中运行：

```powershell
Set-Location 'D:\Projects\粤社多邻国'
.\scripts\ota.ps1
```

脚本默认使用你已验证的连接：

- 服务器：`admin@118.178.91.193`
- SSH端口：`22`
- 私钥：`C:\Users\33941\Downloads\root.pem`

脚本会检查本地项目、生成完整上传包及每个文件的SHA-256清单，通过SSH上传，再校验服务器文件。只有校验通过才切换到新网页；已经上线时还会验证HTTPS返回的入口文件。若这一步失败，会切回原来的网页。

脚本使用本机Node.js、OpenSSH和tar，服务器使用Python 3。上述工具已在实际部署中验证可用，不需要在服务器安装Node.js或给服务器配置GitHub凭据。

## 修改录音、广告或二维码后

先按 [素材说明](materials/素材说明.md) 放好素材，然后运行：

```powershell
npm run materials
.\scripts\ota.ps1
```

仅修改代码或直接编辑 `dist/content/course.js` 时，可以直接运行OTA脚本。脚本不会自行执行素材接入命令，避免意外重建你已经维护好的素材清单。

如果需要获取GitHub上的新提交，可以在本地改动已妥善保存后先运行 `git pull --ff-only`，再执行OTA。发布内容始终来自本机的 `dist/`。

上传结束后打开线上地址刷新检查。网页响应使用 `Cache-Control: no-cache`，浏览器会验证缓存是否仍有效。正在答题的页面不会被强制刷新；用户主动刷新会清空当前答题进度。[Nginx缓存指令说明](https://nginx.org/en/docs/http/ngx_http_headers_module.html#expires)

## 回退刚才的更新

如果发布后发现题库、样式或素材不合适，执行：

```powershell
.\scripts\ota.ps1 -Rollback
```

服务端保留当前内容和一份更新前备份。每次成功更新后，原来在线的内容成为备份，更早的备份被替换；首次部署还没有备份，后续成功更新后即可回退。回退也会先验证备份文件及HTTPS内容。

文件校验失败时不会切换网页；切换后的HTTP检查失败则会切回原网页。若更新在替换旧备份后失败，当前网页保留，但可能暂时没有可用的回退副本；下一次成功更新会重新建立备份。

## 更换私钥路径或端口

```powershell
.\scripts\ota.ps1 -KeyPath 'C:\你的路径\root.pem' -Port 22
```

服务器、域名和Nginx路径与发布程序配套；不要只修改 `-Server` 就部署到另一台未配置的服务器。更换电脑后，需要先用SSH手动核对主机指纹并测试连接，再运行脚本。

脚本默认非交互执行；如果密钥有口令，由你先在本机SSH agent中解锁。当前服务器已验证允许完成部署所需的非交互sudo操作。

## 实际服务器文件

| 路径 | 用途 |
| --- | --- |
| `/etc/nginx/sites-available/conscient-blog` | 博客的实际Nginx配置 |
| `/etc/nginx/snippets/yue-location.conf` | 粤社应用的路径规则 |
| `/var/www/zju-cantonese-duolingo` | Nginx读取的网页入口，是指向当前内容的符号链接 |
| `/var/lib/zju-cantonese-duolingo/site-a`、`site-b` | 轮流存放当前内容及一份更新前备份 |
| `/var/lib/zju-cantonese-duolingo/state.json` | 当前目录和回退目录记录 |
| `/var/backups/zju-cantonese-duolingo/conscient-blog.before-yue.conf` | 首次接入应用前的博客配置备份 |

更新使用两个固定目录轮换，最后原子替换网页入口的符号链接。上传和校验期间线上继续读取原目录。目录名是内部存储位置，不是项目版本编号。

不要手动将文件复制进网页入口，否则会绕过完整性校验和回退记录。后续更新统一使用 `scripts/ota.ps1`。

## Nginx配置与维护

博客HTTPS的 `server` 块中已加入：

```nginx
include /etc/nginx/snippets/yue-location.conf;
```

片段内容与仓库的 [deploy/yue-location.conf](deploy/yue-location.conf) 一致：`/yue` 跳转到 `/yue/`，`/yue/` 通过 `alias` 对应网页入口。原有证书、HTTP跳转和博客反代保留。[Nginx alias说明](https://nginx.org/en/docs/http/ngx_http_core_module.html#alias)

普通OTA不修改Nginx，也不需要重载服务。只有修改Nginx配置时，才需要在服务器执行：

```bash
sudo nginx -t && sudo nginx -s reload
```

平滑重载后，新工作进程需要短暂时间接管请求，应等新路径实际返回预期内容再判断部署成功。[Nginx重载说明](https://nginx.org/en/docs/control.html)

如需撤下应用，在博客配置中移除上述include行，检查并重载即可。配置备份用于恢复接入前状态；恢复整份备份前，应确认备份之后没有其他需要保留的博客配置修改。

## 验收

更新后检查：

- https://conscient.hk.cn/yue/ 能加载题目和图片，选择词块正常。
- https://conscient.hk.cn/ 博客正常。
- 接入正式素材后，在活动实际手机中试听，完整看完15秒广告并确认二维码可扫。

当前线上仍使用测试语料，正式社员录音、广告和招新二维码待提供。未提供素材时显示相应占位内容，是预期行为。
