# 在现有 Ubuntu + Nginx 博客服务器上部署

目标访问地址：`https://conscient.hk.cn/yue/`。

网页是纯静态应用，直接交给 Nginx 提供文件即可，服务器不需要安装 Node.js、运行 `npm start` 或新增反代端口。原有博客继续使用现有的反代配置；新应用使用 `/yue/` 路径，并沿用当前域名的 HTTPS 证书。

本说明适用于直接安装的 Nginx。下面是待你在服务器执行的步骤；项目没有自动连接服务器或部署。

## 1. 从电脑上传网页和配置片段

以下命令在 Windows PowerShell 执行。把 `你的SSH用户名@你的服务器IP` 换成实际 SSH 登录地址（例如 `ubuntu@203.0.113.10`）。如果 SSH 使用非默认端口，为 `ssh` 添加 `-p 端口`，为 `scp` 添加 `-P 端口`。

```powershell
Set-Location 'D:\Projects\粤社多邻国'
$serverTarget = '你的SSH用户名@你的服务器IP'
ssh $serverTarget 'mkdir -p ~/yue-upload'
scp -r ./dist "${serverTarget}:~/yue-upload/"
scp ./deploy/yue-location.conf "${serverTarget}:~/yue-upload/"
ssh $serverTarget
```

后续命令均在 SSH 登录后的 Ubuntu 终端执行。由于上传的是本机 `dist`，服务器不需要访问私有 GitHub 仓库，也不需要配置 GitHub 凭据。

## 2. 放置网页文件

```bash
sudo install -d -m 755 /var/www/zju-cantonese-duolingo
sudo cp -a ~/yue-upload/dist/. /var/www/zju-cantonese-duolingo/
sudo chmod -R u=rwX,go=rX /var/www/zju-cantonese-duolingo
sudo install -D -m 644 ~/yue-upload/yue-location.conf /etc/nginx/snippets/yue-location.conf
```

部署后的文件应是 `/var/www/zju-cantonese-duolingo/index.html`，而非该目录下面再套一层 `dist`。这个目录仅放 `dist` 里的网页文件，仓库、材料交付目录和 `.git` 不放入网站公开目录。

## 3. 给现有博客配置添加两条路径规则

先查看当前生效的配置，找到 `server_name conscient.hk.cn` 且监听 HTTPS 的 `server` 块；输出中的 `# configuration file ...` 会标明它所在的文件。

```bash
sudo nginx -T
```

Ubuntu 常见配置位置是 `/etc/nginx/sites-available/` 或 `/etc/nginx/conf.d/`，以你的实际输出为准。把下方路径换成找到的文件路径，再备份和编辑：

```bash
site_conf='/etc/nginx/sites-available/你的博客配置文件'
sudo cp "$site_conf" /root/nginx-blog-before-yue.conf
sudo nano "$site_conf"
```

在原有 **HTTPS `server { ... }` 块内部**添加这一行，与博客的 `location / { ... }` 同级：

```nginx
include /etc/nginx/snippets/yue-location.conf;
```

不要把它放入现有 `location /` 内，也不必添加新的 `server` 块。保留原有 `listen`、证书、`server_name` 和博客 `proxy_pass`。

被引入的片段如下，项目的 `deploy/yue-location.conf` 已经写好：

```nginx
location = /yue {
    return 301 /yue/;
}

location ^~ /yue/ {
    alias /var/www/zju-cantonese-duolingo/;
    index index.html;
    autoindex off;
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    expires -1;
}
```

`alias` 将 `/yue/` 对应到网页目录，两边结尾的 `/` 都要保留。`^~` 让这一段路径优先于常见的博客静态资源正则规则。项目使用相对资源地址，可以直接放到这个子路径。[Nginx location 与 alias 文档](https://nginx.org/en/docs/http/ngx_http_core_module.html#alias)

这里的 `expires -1` 使浏览器复用缓存前先验证，便于后续更新同名脚本和语料，避免长时间停留在旧版本。[Nginx expires 文档](https://nginx.org/en/docs/http/ngx_http_headers_module.html#expires)

## 4. 检查配置并平滑重载

```bash
sudo nginx -t && sudo systemctl reload nginx
```

只有配置检查通过才会执行重载；如果检查报错，按报错的文件和行号修正后再执行。Nginx 的 reload 会平滑切换工作进程。[Nginx 命令说明](https://nginx.org/en/docs/switches.html)

## 5. 验收网页和博客

```bash
curl -I https://conscient.hk.cn/yue/
curl -I https://conscient.hk.cn/yue/game.js
curl -I https://conscient.hk.cn/yue/content/course.js
curl -I https://conscient.hk.cn/
```

前三个地址应返回成功状态，两个 JS 文件的 `Content-Type` 应为 JavaScript 类型；博客首页应保持原来的正常结果。再用手机打开 `https://conscient.hk.cn/yue/`，确认题目、角色图片和按钮正常。正式录音、广告与二维码接入后，再在实际手机中试听、看完广告并扫码。

常见情况：

- `/yue/` 出现博客页面：片段可能加入了错误的 `server`，或未成功重载；检查当前域名实际命中的 HTTPS 配置及是否有 server 级重写。
- 403：检查网页目录与所有上级目录对 Nginx 工作进程是否可读、可进入。
- 404：检查 `index.html` 是否就在部署目录中、`alias` 路径是否正确，以及是否误套了一层 `dist`。
- JS 加载成 HTML：检查请求是否落到了博客的回退路由；此应用的 JS 请求应由 `/yue/` 静态规则响应。
- 录音提示“待补充”：当前仓库尚无正式录音，是预期的占位行为。

## 6. 后续更新

先在电脑上更新项目；如果其他地方有新的 GitHub 提交，在没有未提交修改时运行 `git pull --ff-only`。替换社员素材后运行：

```powershell
npm run materials
npm run check
```

重新运行第1步中上传 `dist` 的命令，在服务器重复第2步的网页复制和权限命令即可。仅更新网页、录音、广告或二维码时无需重载 Nginx；只有修改 Nginx 配置时才需要第4步。

复制会覆盖同名文件、保留旧文件；若主动删除或改名了素材，需要按实际文件名清理部署目录里的对应旧文件。不要对博客目录操作。

## 回退

如需撤下新应用，在博客配置中移除新增的 `include /etc/nginx/snippets/yue-location.conf;`，检查配置并重载。也可以在确认备份之后没有其他配置修改的情况下，恢复 `/root/nginx-blog-before-yue.conf` 到原配置路径。博客反代本身无需改动。

多位用户的答题进度、成绩和用时在各自页面内独立计算，刷新会重新开始。服务端只提供文件，没有共享答题状态；实际承载人数主要受带宽和音视频大小影响。
